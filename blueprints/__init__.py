# This package groups the app's routes into blueprints:
#   auth       -> login / register / password recovery
#   listings   -> create / browse / enroll / close listings
#   messaging  -> user-to-user direct messages
#   admin      -> admin-only database maintenance page
